spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: server
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

ssh:
  host: 192.168.1.14